{
  "name": "agentproof",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pre-acceptance verifier for OKX.AI A2A task deliverables.",
  "scripts": {
    "build": "node build.js",
    "preview": "npx wrangler pages dev dist --compatibility-date=2026-07-03",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name agentproof"
  }
}
